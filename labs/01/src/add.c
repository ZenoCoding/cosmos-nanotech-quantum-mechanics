#include <stdio.h>
#include <math.h>
int main(void){
    double x, y, z;
    printf("Enter x: ");
    scanf("%lf", &x);
    printf("Enter y: ");
    scanf("%lf", &y);
    z = x + y;
    printf("x + y = %g \n5", z);
    return 0;
}