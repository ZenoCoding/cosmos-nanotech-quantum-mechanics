# Lab 0 — Setup and Smoke Tests

**Reconstruction confidence: medium.**

This folder contains the smallest programs and the original editor
configuration, making it the best fit for an environment/setup lab.

- `hello.c` verifies the compiler with “Hello, world”.
- `snake.c` is an unfinished timing/sleep experiment retained as coursework.
- `.vscode/` contains the original build, launch, and C configuration.

Build with `make lab00`.
